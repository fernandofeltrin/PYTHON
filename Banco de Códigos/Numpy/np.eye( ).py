# Crie uma array Numpy em formato de matriz transversa / matriz de identidade / matriz diagonal, contendo 10 elementos na diagonal, todos de tipo inteiro
array = np.eye(N = 10,
               dtype='int')  
     
print(array)
