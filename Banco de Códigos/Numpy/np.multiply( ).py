# Dadas duas arrays Numpy iniciais, crie uma terceira array composta pelo resultado da multiplicação dos elementos das duas anteriores. 
A = np.array([[3, 4, 9, 2], [5, 3, 2, 5]])
B = np.array([[4, 3, 2, 5], [6, 3, 1, 6]])

C = np.multiply(A, B)
 
print(C)
