# Uma vez que temos uma array Numpy [2, 1, 6, 4, 5, 3], retorne a referência dos índices que ordenariam de forma crescente os elementos da array
array = np.array([2, 1, 6, 4, 5, 3])
print(array)
 
print(np.argsort(array))
