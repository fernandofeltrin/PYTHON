# Fornecida a array Numpy [[3, 4, 9, 2], [5, 3, 2, 5]], crie uma nova array baseada na raiz quadrada de cada um dos elementos da array de origem.
A = np.array([[3, 4, 9, 2], [5, 3, 2, 5]])
B = np.sqrt(A)

print(B)
