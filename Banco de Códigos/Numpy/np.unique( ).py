# Extraia todos os elementos de uma array para uma lista simples, porém, os elementos devem estar ordenados numericamente de forma crescente.
A = np.random.randint(low=1, high=10, size=(2, 3))

print(A)
print(np.unique(A))
